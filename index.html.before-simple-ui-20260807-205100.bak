<!doctype html>
<html lang="ja">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="BMSのScore.dbからStella_Ratingと統合レーティングを確認できるブラウザツールです。">
    <meta name="theme-color" content="#070b18">
    <title>Stella Rating Hub</title>
    <link rel="icon" href="SR_icon.png" type="image/png">
    <style>
        :root {
            color-scheme: dark;
            --bg: #070b18;
            --panel: rgba(17, 24, 48, 0.78);
            --line: rgba(255, 255, 255, 0.12);
            --text: #f6f7ff;
            --muted: #aeb8d4;
            --blue: #6cd8ff;
            --violet: #b99cff;
        }

        * { box-sizing: border-box; }

        html { min-width: 320px; }

        body {
            min-height: 100vh;
            margin: 0;
            color: var(--text);
            font-family: system-ui, -apple-system, "Segoe UI", "Noto Sans JP", sans-serif;
            background:
                radial-gradient(circle at 12% 8%, rgba(63, 124, 255, 0.2), transparent 30rem),
                radial-gradient(circle at 88% 22%, rgba(145, 77, 255, 0.18), transparent 28rem),
                linear-gradient(145deg, #050713 0%, var(--bg) 52%, #0c1022 100%);
        }

        body::before {
            position: fixed;
            inset: 0;
            z-index: -1;
            content: "";
            opacity: 0.42;
            background-image:
                radial-gradient(circle, rgba(255, 255, 255, 0.7) 0 1px, transparent 1.5px),
                radial-gradient(circle, rgba(149, 203, 255, 0.55) 0 1px, transparent 1.5px);
            background-position: 0 0, 37px 51px;
            background-size: 92px 92px, 137px 137px;
            mask-image: linear-gradient(to bottom, black, transparent 76%);
        }

        .page {
            width: min(1100px, 100%);
            margin: 0 auto;
            padding: clamp(28px, 6vw, 72px) clamp(18px, 4vw, 44px) 32px;
        }

        .brand {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            color: #dce5ff;
            font-size: 0.82rem;
            font-weight: 750;
            letter-spacing: 0.16em;
            text-transform: uppercase;
        }

        .brand img {
            width: 42px;
            height: 42px;
            border: 1px solid rgba(255, 255, 255, 0.22);
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.28);
        }

        .hero { padding: clamp(42px, 8vw, 86px) 0 clamp(36px, 6vw, 64px); }

        .eyebrow {
            margin: 0 0 14px;
            color: var(--blue);
            font-size: 0.78rem;
            font-weight: 800;
            letter-spacing: 0.18em;
        }

        h1 {
            max-width: 850px;
            margin: 0;
            font-size: clamp(2.5rem, 8vw, 5.5rem);
            line-height: 1.03;
            letter-spacing: -0.055em;
        }

        h1 span {
            color: transparent;
            background: linear-gradient(100deg, var(--blue), #d8c7ff 58%, #ffb4db);
            background-clip: text;
            -webkit-background-clip: text;
        }

        .lead {
            max-width: 690px;
            margin: 24px 0 0;
            color: var(--muted);
            font-size: clamp(1rem, 2vw, 1.15rem);
            line-height: 1.9;
        }

        .cards {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 20px;
        }

        .card {
            position: relative;
            isolation: isolate;
            display: flex;
            min-height: 330px;
            padding: clamp(24px, 4vw, 34px);
            overflow: hidden;
            flex-direction: column;
            color: var(--text);
            text-decoration: none;
            border: 1px solid var(--line);
            border-radius: 26px;
            background: linear-gradient(145deg, rgba(29, 42, 78, 0.88), var(--panel));
            box-shadow: 0 22px 55px rgba(0, 0, 0, 0.24);
            transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
        }

        .card::after {
            position: absolute;
            z-index: -1;
            top: -95px;
            right: -75px;
            width: 250px;
            height: 250px;
            content: "";
            border-radius: 50%;
            background: rgba(63, 184, 255, 0.24);
            filter: blur(3px);
        }

        .card--unified {
            background: linear-gradient(145deg, rgba(54, 34, 91, 0.9), var(--panel));
        }

        .card--unified::after { background: rgba(186, 105, 255, 0.3); }

        .card:focus-visible {
            outline: 3px solid #fff;
            outline-offset: 4px;
        }

        .tag {
            align-self: flex-start;
            padding: 6px 10px;
            color: #d8f5ff;
            font-size: 0.7rem;
            font-weight: 800;
            letter-spacing: 0.12em;
            border: 1px solid rgba(108, 216, 255, 0.36);
            border-radius: 999px;
            background: rgba(108, 216, 255, 0.08);
        }

        .card--unified .tag {
            color: #eadfff;
            border-color: rgba(185, 156, 255, 0.42);
            background: rgba(185, 156, 255, 0.1);
        }

        .card h2 {
            margin: 24px 0 10px;
            font-size: clamp(1.65rem, 4vw, 2.25rem);
            letter-spacing: -0.025em;
        }

        .card p {
            margin: 0;
            color: var(--muted);
            line-height: 1.75;
        }

        .facts {
            display: flex;
            margin: 22px 0 28px;
            padding: 0;
            flex-wrap: wrap;
            gap: 8px;
            list-style: none;
        }

        .facts li {
            padding: 6px 9px;
            color: #dce5ff;
            font-size: 0.78rem;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.07);
        }

        .action {
            display: flex;
            margin-top: auto;
            align-items: center;
            justify-content: space-between;
            color: #fff;
            font-weight: 750;
        }

        .arrow {
            display: grid;
            width: 38px;
            height: 38px;
            place-items: center;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.11);
            transition: transform 180ms ease, background 180ms ease;
        }

        .notes {
            display: grid;
            margin-top: 24px;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 12px;
        }

        .note {
            padding: 17px 19px;
            color: var(--muted);
            font-size: 0.88rem;
            line-height: 1.65;
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 14px;
            background: rgba(9, 14, 31, 0.6);
        }

        .note strong { color: #edf1ff; }

        footer {
            padding-top: 46px;
            color: #7f8aa8;
            font-size: 0.76rem;
            text-align: center;
        }

        @media (hover: hover) {
            .card:hover {
                transform: translateY(-5px);
                border-color: rgba(255, 255, 255, 0.25);
                box-shadow: 0 28px 64px rgba(0, 0, 0, 0.34);
            }

            .card:hover .arrow {
                transform: translateX(3px);
                background: rgba(255, 255, 255, 0.2);
            }
        }

        @media (max-width: 720px) {
            .cards, .notes { grid-template-columns: 1fr; }
            .card { min-height: 300px; }
            .hero { padding-top: 48px; }
        }

        @media (prefers-reduced-motion: reduce) {
            .card, .arrow { transition: none; }
        }
    </style>
</head>
<body>
    <div class="page">
        <header>
            <div class="brand">
                <img src="SR_icon.png" alt="" width="42" height="42">
                <span>Stella Rating Hub</span>
            </div>
            <div class="hero">
                <p class="eyebrow">BMS SCORE ANALYZER</p>
                <h1>あなたのスコアを、<br><span>二つの視点で。</span></h1>
                <p class="lead">Score.dbを読み込み、従来のStella_Ratingと、3つの難易度表をつないだ統合レーティングを確認できます。</p>
            </div>
        </header>

        <main>
            <section class="cards" aria-label="評価ツールを選ぶ">
                <a class="card" href="stella-rating.html">
                    <span class="tag">ORIGINAL</span>
                    <h2>Stella_Rating</h2>
                    <p>Satellite・Stella難易度表を使い、クリアランプと判定から従来式のTotal Ratingを算出します。</p>
                    <ul class="facts" aria-label="主な機能">
                        <li>SL0〜SL12</li>
                        <li>ST0〜ST12</li>
                        <li>上位30譜面</li>
                    </ul>
                    <span class="action">従来の評価を開く <span class="arrow" aria-hidden="true">→</span></span>
                </a>

                <a class="card card--unified" href="unified-rating.html">
                    <span class="tag">NEW</span>
                    <h2>統合レーティング</h2>
                    <p>Starlight・Satellite・Stellaを統合難易度1〜38として扱い、精度ベースの単曲レーティングを算出します。</p>
                    <ul class="facts" aria-label="主な機能">
                        <li>SR・SL・ST</li>
                        <li>統合難易度 1〜38</li>
                        <li>上位200譜面</li>
                    </ul>
                    <span class="action">統合評価を開く <span class="arrow" aria-hidden="true">→</span></span>
                </a>
            </section>

            <aside class="notes" aria-label="利用時の注意">
                <div class="note"><strong>スコアは外部送信されません。</strong><br>選択したScore.dbは、お使いのブラウザ内だけで読み込み・計算します。</div>
                <div class="note"><strong>GitHub Pagesからご利用ください。</strong><br>HTMLをPC上で直接開くと、ブラウザの制限により難易度表データを読み込めません。</div>
            </aside>
        </main>

        <footer>Stella Rating Hub — BMS scores, calculated locally.</footer>
    </div>
</body>
</html>
